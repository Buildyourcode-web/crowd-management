version: "3.8"

# ══════════════════════════════════════════════════════════════════════
# Temple AI Crowd Management System — Supabase Cloud Docker Compose
# ══════════════════════════════════════════════════════════════════════

services:

  # ── Redis (In-Memory Pub/Sub & Caching) ───────────────────────────
  redis:
    image: redis:7-alpine
    container_name: temple_redis
    restart: always
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── FastAPI Backend (AI Analytics & Supabase Database) ─────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: temple_backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: "010d67237526fbafce54cd1528959fc6441325c1d807bcb00f2f6d54e74e2dec"
      POSTGRES_HOST: "db.mtysplndnfjssgqvomkh.supabase.co"
      POSTGRES_PORT: 5432
      POSTGRES_DB: "postgres"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "Ramkumar@039"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      ENVIRONMENT: production
      DEBUG: "false"
      ALLOWED_ORIGINS: '["*"]'
    volumes:
      - ./backend/models:/app/models
      - ./backend/logs:/app/logs
    depends_on:
      redis:
        condition: service_healthy

  # ── React Frontend (Web Dashboard & CCTV Wall) ────────────────────
  frontend:
    build:
      context: ./react-frontend
      dockerfile: Dockerfile
    container_name: temple_frontend
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend
